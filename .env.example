# App
APP_NAME=AI House Planner
APP_ENV=local
BASE_URL=http://localhost

# AI House Planner
# OPENAI_API_KEY=sk-...   (valgfritt — uten key er "Generate plan" deaktivert)
# BASE_PATH=               (valgfritt — f.eks. /main-projects/aihouseplanner/public)

# Database
DB_DSN=mysql:host=localhost;dbname=min_app;charset=utf8mb4
DB_USER=root
DB_PASS=

# Auth-webtjeneste (valgfritt til integrering)
# AUTH_URL=https://auth.example.com
# JWT_PUBLIC_KEY=
# JWT_ALGORITHM=RS256
